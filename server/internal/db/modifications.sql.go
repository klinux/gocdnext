// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: modifications.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getModificationByKey = `-- name: GetModificationByKey :one
SELECT id, material_id, revision, branch, author, message, payload, committed_at, detected_at
FROM modifications
WHERE material_id = $1 AND revision = $2 AND branch IS NOT DISTINCT FROM $3
LIMIT 1
`

type GetModificationByKeyParams struct {
	MaterialID pgtype.UUID
	Revision   string
	Branch     *string
}

func (q *Queries) GetModificationByKey(ctx context.Context, arg GetModificationByKeyParams) (Modification, error) {
	row := q.db.QueryRow(ctx, getModificationByKey, arg.MaterialID, arg.Revision, arg.Branch)
	var i Modification
	err := row.Scan(
		&i.ID,
		&i.MaterialID,
		&i.Revision,
		&i.Branch,
		&i.Author,
		&i.Message,
		&i.Payload,
		&i.CommittedAt,
		&i.DetectedAt,
	)
	return i, err
}

const insertModification = `-- name: InsertModification :one
INSERT INTO modifications (
    material_id, revision, branch, author, message, payload, committed_at
) VALUES (
    $1, $2, $3, $4, $5, $6, $7
)
ON CONFLICT (material_id, revision, branch) DO NOTHING
RETURNING id, material_id, revision, branch, author, message, payload, committed_at, detected_at
`

type InsertModificationParams struct {
	MaterialID  pgtype.UUID
	Revision    string
	Branch      *string
	Author      *string
	Message     *string
	Payload     []byte
	CommittedAt pgtype.Timestamptz
}

func (q *Queries) InsertModification(ctx context.Context, arg InsertModificationParams) (Modification, error) {
	row := q.db.QueryRow(ctx, insertModification,
		arg.MaterialID,
		arg.Revision,
		arg.Branch,
		arg.Author,
		arg.Message,
		arg.Payload,
		arg.CommittedAt,
	)
	var i Modification
	err := row.Scan(
		&i.ID,
		&i.MaterialID,
		&i.Revision,
		&i.Branch,
		&i.Author,
		&i.Message,
		&i.Payload,
		&i.CommittedAt,
		&i.DetectedAt,
	)
	return i, err
}
