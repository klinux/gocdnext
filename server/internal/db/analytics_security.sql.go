// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: analytics_security.sql

package db

import (
	"context"
)

const securityRollupAccepted = `-- name: SecurityRollupAccepted :many
WITH latest AS (
    SELECT DISTINCT ON (sc.pipeline_id, sc.scanner_job, sc.matrix_key)
        sc.pipeline_id, sc.scanner_job, sc.matrix_key, sc.run_id AS latest_run_id
    FROM security_scans sc
    JOIN runs r ON r.id = sc.run_id
    ORDER BY sc.pipeline_id, sc.scanner_job, sc.matrix_key, r.counter DESC
)
SELECT pl.value AS grp, COUNT(*)::bigint AS n
FROM security_finding_states sfs
JOIN latest l
    ON  l.pipeline_id = sfs.pipeline_id
    AND l.scanner_job = sfs.scanner_job
    AND l.matrix_key  = sfs.matrix_key
JOIN project_labels pl ON pl.project_id = sfs.project_id AND pl.key = $1
WHERE sfs.last_seen_run_id = l.latest_run_id
  AND sfs.state = 'accepted'
GROUP BY pl.value
`

type SecurityRollupAcceptedRow struct {
	Grp string
	N   int64
}

// Accepted-risk identities per group (shown as a distinct count, never folded
// into the open severity breakdown). Same present condition as the open counts.
func (q *Queries) SecurityRollupAccepted(ctx context.Context, labelKey string) ([]SecurityRollupAcceptedRow, error) {
	rows, err := q.db.Query(ctx, securityRollupAccepted, labelKey)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SecurityRollupAcceptedRow{}
	for rows.Next() {
		var i SecurityRollupAcceptedRow
		if err := rows.Scan(&i.Grp, &i.N); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const securityRollupCounts = `-- name: SecurityRollupCounts :many
WITH latest AS (
    SELECT DISTINCT ON (sc.pipeline_id, sc.scanner_job, sc.matrix_key)
        sc.pipeline_id, sc.scanner_job, sc.matrix_key, sc.run_id AS latest_run_id
    FROM security_scans sc
    JOIN runs r ON r.id = sc.run_id
    ORDER BY sc.pipeline_id, sc.scanner_job, sc.matrix_key, r.counter DESC
)
SELECT pl.value AS grp, sfs.last_severity AS severity, COUNT(*)::bigint AS n
FROM security_finding_states sfs
JOIN latest l
    ON  l.pipeline_id = sfs.pipeline_id
    AND l.scanner_job = sfs.scanner_job
    AND l.matrix_key  = sfs.matrix_key
JOIN project_labels pl ON pl.project_id = sfs.project_id AND pl.key = $1
WHERE sfs.last_seen_run_id = l.latest_run_id
  AND sfs.state = 'open'
GROUP BY pl.value, sfs.last_severity
`

type SecurityRollupCountsRow struct {
	Grp      string
	Severity string
	N        int64
}

// Open identities per (label-value group, severity). The latest CTE is the
// latest reconciled run per (pipeline, scanner_job, matrix_key) across all
// projects; an identity counts when it's still present in that latest scan and
// open. Dedupe is intrinsic (one row per identity in security_finding_states).
func (q *Queries) SecurityRollupCounts(ctx context.Context, labelKey string) ([]SecurityRollupCountsRow, error) {
	rows, err := q.db.Query(ctx, securityRollupCounts, labelKey)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SecurityRollupCountsRow{}
	for rows.Next() {
		var i SecurityRollupCountsRow
		if err := rows.Scan(&i.Grp, &i.Severity, &i.N); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const securityRollupGroups = `-- name: SecurityRollupGroups :many

SELECT pl.value AS grp,
       EXISTS (
           SELECT 1
           FROM security_scans sc
           JOIN pipelines p ON p.id = sc.pipeline_id
           JOIN project_labels pl2 ON pl2.project_id = p.project_id
           WHERE pl2.key = $1 AND pl2.value = pl.value
       ) AS has_scans
FROM project_labels pl
WHERE pl.key = $1
GROUP BY pl.value
ORDER BY pl.value
`

type SecurityRollupGroupsRow struct {
	Grp      string
	HasScans bool
}

// Org/label security rollup (#71 v3). Counts finding IDENTITIES (not SARIF
// occurrences) from security_finding_states: an identity is "currently open"
// when it's present in its scanner's latest reconciled scan
// (last_seen_run_id = latest_run_id) and state='open'. Grouped by a project
// label value.
// The group spine: every label value for the key, plus whether any of the
// group's projects has ever reconciled a scan (clean-vs-never-scanned). Counts
// are LEFT-merged in Go so a clean/zero group still appears.
func (q *Queries) SecurityRollupGroups(ctx context.Context, labelKey string) ([]SecurityRollupGroupsRow, error) {
	rows, err := q.db.Query(ctx, securityRollupGroups, labelKey)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SecurityRollupGroupsRow{}
	for rows.Next() {
		var i SecurityRollupGroupsRow
		if err := rows.Scan(&i.Grp, &i.HasScans); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
